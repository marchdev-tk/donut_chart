name: donut_chart_example
description: Demonstrates how to use the donut_chart package.

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  donut_chart:
    path: ../

flutter:
  uses-material-design: true
