name: donut_chart
version: 0.4.0
homepage: https://github.com/marchdev-tk/donut_chart
description: Donut chart

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  pedantic: ^1.11.0

flutter:
  uses-material-design: true
